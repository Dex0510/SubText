{
  "name": "subtext",
  "version": "2.1.0",
  "private": true,
  "description": "Forensic Relationship Intelligence Platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:worker\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:worker": "cd backend && npm run dev:worker",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "docker:up": "docker compose up -d postgres redis",
    "docker:down": "docker compose down",
    "docker:full": "docker compose up -d --build",
    "setup": "npm run docker:up && cd backend && npm install && cd ../frontend && npm install",
    "typecheck": "cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
